apiVersion: v1
kind: Service
metadata:
  name: {{ include "squash-webhook.fullname" . }}
  labels:
    {{- include "squash-webhook.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: webhook
      protocol: TCP
      name: https
  selector:
    {{- include "squash-webhook.selectorLabels" . | nindent 4 }}
