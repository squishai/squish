# ── Core inference ────────────────────────────────────────────────────────────
mlx>=0.18
mlx-lm>=0.19
safetensors
numpy>=1.26
transformers>=4.40
huggingface_hub>=0.23

# ── OpenAI-compatible API server (server.py) ──────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29.0
sse-starlette>=1.8.2

# ── Entropy compression / transparent .npy.zst loading ───────────────────────
zstandard>=0.22.0

# ── Industry-standard benchmarks (run_eval.py / squish_lm_eval.py) ───────────
lm-eval>=0.4.0
datasets>=2.18
accelerate>=0.29
sacrebleu
rouge_score
nltk

# ── Constrained generation for tool calling (tool_calling_demo.py) ────────────
# outlines[mlxlm]   # uncomment when ready

# ── Pydantic for schema validation (benchmark.py) ─────────────────────────────
# pydantic>=2.0
